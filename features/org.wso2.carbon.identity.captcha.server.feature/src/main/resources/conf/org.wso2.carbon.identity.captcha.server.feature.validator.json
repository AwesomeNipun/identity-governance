{
  "recaptcha.site_key": [
    {
      "if": {
        "recaptcha.enabled": true
      },
      "regex": ".+",
      "error_message": "Recaptcha site key is required."
    }
  ],

  "recaptcha.secret_key": [
    {
      "if": {
        "recaptcha.enabled": true
      },
      "regex": ".+",
      "error_message": "Recaptcha secret key is required."
    }
  ],

  "hcaptcha.site_key": [
    {
      "if": {
        "hcaptcha.enabled": true
      },
      "regex": ".+",
      "error_message": "hcaptcha site key is required."
    }
  ],

  "hcaptcha.secret_key": [
    {
      "if": {
        "hcaptcha.enabled": true
      },
      "regex": ".+",
      "error_message": "hcaptcha secret key is required."
    }
  ]
}